# INPSan Official Checkpoint — CP-20260908-UI2-R10-01

**Title:** Dashboard DOM Structure, Alert Actions and Hardware Aggregate Baseline  
**Recorded:** 2026-09-08  
**Status:** Dashboard visual acceptance PASS; alert-action and FMA policy acceptance pending

## Accepted baseline

- Web GUI: `v3.1.13`
- Dashboard/DataAdapter: `v3.2.4.6`; DataAdapter SHA-256 baseline retained
- Dashboard Alert Action and Hardware Aggregate Hotfix: `v3.2.4.6-ui2-r1`
- Alert Engine aggregate hotfix: `v3.2.3.0-r3-r2`
- Alert schema: `1.1`; Event schema: `1.0`; Operations schema: `1.8`
- H240 Topology: `v3.2.0.11-r4`; physical topology contract: `24/24` verified slots
- iLO/Redfish telemetry: extension `3.2.0.12`, connected/fresh when last sampled
- Notification outbound channels: disabled

## Completed and verified

- Dashboard HTML structure corrected: removed one extra closing `</div>` from the Notification modal.
- Browser verification passed:

  `#inpsan-historical-dashboard` contains `#inpsan-chassis-visualization` = `true`.

- Web dashboard was visually rechecked and accepted as correctly styled.
- Alert action renderer was hardened for Acknowledge/Silence requests and inline result/error reporting.
- Generic `Server hardware health requires review` aggregation was de-duplicated when an independent FMA condition is already present; real component conditions remain preserved.
- iLO Redfish showed two healthy PSUs, healthy severity and fresh telemetry in the latest evidence.
- ZFS pools remained healthy; no pool, disk, Event Store, credential, notification or outbound state was changed.
- Package native verification, syntax checks, UI regression and FMA aggregate regression: PASS.

## Known active or pending conditions

- FMA condition remains an independent active/acknowledged alert and requires evidence-based RCA using `fmadm`/`fmdump`; it must not be cleared solely for cosmetic reasons.
- Acknowledge/Silence workflow still requires one final GUI acceptance test against the FMA and another active alert, including audit/event confirmation after the next engine cycle.
- Hardware-health severity must be reconciled with component data so healthy iLO/PSU telemetry does not create a misleading generic critical state.
- If Physical Bay alerts recur, capture topology, H240 and Alert Engine samples before any hardware or pool action.

## Safety and change boundaries

- No reboot is required for the dashboard DOM correction.
- Do not enable SMTP, Webhook or Syslog outbound channels.
- Do not alter ZFS pools, LUs, Event Store history or topology mappings during RCA.
- Any policy change must be preceded by a captured source sample and a rollback copy.

## Next continuation path

1. **P1 — Alert-action acceptance:** test GUI Acknowledge and Silence, then verify `alertctl`, audit JSONL and event transition after one engine cycle.
2. **P2 — FMA RCA and classification:** inspect `fmadm faulty`, `fmadm list`, and `fmdump` evidence; classify as active fault, historical condition or false positive.
3. **P3 — Hardware-health policy reconciliation:** align aggregate severity with authoritative iLO/component data while preserving genuine FMA and component alerts.
4. **P4 — Physical-Bay stability watch:** run a bounded watcher and collect evidence if disk presence/enumeration flaps recur; no destructive remediation.
5. **P5 — Final acceptance:** update Baseline Register, Issue/RCA, ADR, Test & Evidence, Release Notes and produce the next official checkpoint.

## Release artifact

- Package: `INPSan-Dashboard-Alert-Action-and-Hardware-Aggregate-Hotfix-v3.2.4.6-ui2-r10-OmniOS-USTAR-2026-09-08.tar.gz`
- SHA-256: `3555ade95b96bbb5c19ab5f26550d51e1ee02f46a42c879155e0b2ad0769659d`

## Safe continuation point

`P1 — Alert-action acceptance, followed by FMA RCA and hardware-health policy reconciliation.`
